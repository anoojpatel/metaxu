"""glade: the Metaxu package manager (docs/glade.md).

A glade is a clearing in a wood: the open ground where paths from
different directions meet and you can see what is there. Packages from
many repositories meet in one lockfile the same way.

    semver     versions and version requirements (^, ~, >=, <, =, *)
    pubgrub    the version solver (the algorithm Cargo, uv and pub use)
    index      the registry: a git repository of package descriptions
    project    manifest, lockfile, resolution, vendoring
    cli        the `glade` command
"""
