fn main() -> int {
    let n = 5000000;
    let mut v = Vec.new();
    let mut i = 0;
    while i < n { v.push(0); i = i + 1 };
    let mut r = 0;
    while r < 4 {
        let mut j = 0;
        while j < n { v[j] = j; j = j + 1 };
        r = r + 1
    };
    print(v[n - 1]);
    0
}
